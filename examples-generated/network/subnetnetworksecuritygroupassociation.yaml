apiVersion: network.azure.jet.crossplane.io/v1alpha1
kind: SubnetNetworkSecurityGroupAssociation
metadata:
  name: example
spec:
  forProvider:
    networkSecurityGroupId: ${azurerm_network_security_group.example.id}
    subnetIdRef:
      name: example
  providerConfigRef:
    name: example
