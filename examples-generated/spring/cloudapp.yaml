apiVersion: spring.azure.jet.crossplane.io/v1alpha1
kind: CloudApp
metadata:
  name: example
spec:
  forProvider:
    identity:
    - type: SystemAssigned
    name: example-springcloudapp
    resourceGroupName: ${azurerm_resource_group.example.name}
    serviceName: example-springcloud
  providerConfigRef:
    name: example
