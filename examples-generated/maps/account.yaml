apiVersion: maps.azure.jet.crossplane.io/v1alpha1
kind: Account
metadata:
  name: example
spec:
  forProvider:
    name: example-maps-account
    resourceGroupName: ${azurerm_resource_group.example.name}
    skuName: S1
    tags:
      environment: Test
  providerConfigRef:
    name: example
