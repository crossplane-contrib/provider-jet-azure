apiVersion: policy.azure.jet.crossplane.io/v1alpha1
kind: Remediation
metadata:
  name: example
spec:
  forProvider:
    locationFilters:
    - West Europe
    name: example-policy-remediation
    policyAssignmentId: ${azurerm_policy_assignment.example.id}
    scope: ${azurerm_resource_group.example.id}
  providerConfigRef:
    name: example
