apiVersion: app.azure.jet.crossplane.io/v1alpha1
kind: ServiceManagedCertificate
metadata:
  name: example
spec:
  forProvider:
    customHostnameBindingId: ${azurerm_app_service_custom_hostname_binding.example.id}
  providerConfigRef:
    name: example
