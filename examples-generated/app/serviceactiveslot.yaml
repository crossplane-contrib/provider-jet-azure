apiVersion: app.azure.jet.crossplane.io/v1alpha1
kind: ServiceActiveSlot
metadata:
  name: example
spec:
  forProvider:
    appServiceName: example-app-service
    appServiceSlotName: ${random_id.server.hex}
    resourceGroupName: ${azurerm_resource_group.example.name}
  providerConfigRef:
    name: example
