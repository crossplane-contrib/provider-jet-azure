apiVersion: app.azure.jet.crossplane.io/v1alpha1
kind: ServiceSlotVirtualNetworkSwiftConnection
metadata:
  name: example
spec:
  forProvider:
    appServiceId: ${azurerm_app_service.example.id}
    slotName: ${random_id.server.hex}
    subnetId: ${azurerm_subnet.example.id}
  providerConfigRef:
    name: example
