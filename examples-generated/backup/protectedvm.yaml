apiVersion: backup.azure.jet.crossplane.io/v1alpha1
kind: ProtectedVm
metadata:
  name: example
spec:
  forProvider:
    backupPolicyId: ${azurerm_backup_policy_vm.example.id}
    recoveryVaultName: example_recovery_vault
    resourceGroupName: ${azurerm_resource_group.example.name}
    sourceVmId: ${azurerm_virtual_machine.example.id}
  providerConfigRef:
    name: example
