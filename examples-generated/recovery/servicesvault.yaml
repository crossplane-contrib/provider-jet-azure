apiVersion: recovery.azure.jet.crossplane.io/v1alpha1
kind: ServicesVault
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: example_recovery_vault
    resourceGroupName: ${azurerm_resource_group.rg.name}
    sku: Standard
    softDeleteEnabled: true
  providerConfigRef:
    name: example
