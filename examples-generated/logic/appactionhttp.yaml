apiVersion: logic.azure.jet.crossplane.io/v1alpha1
kind: AppActionHttp
metadata:
  name: example
spec:
  forProvider:
    logicAppId: ${azurerm_logic_app_workflow.example.id}
    method: GET
    name: webhook
    uri: http://example.com/some-webhook
  providerConfigRef:
    name: example
