apiVersion: api.azure.jet.crossplane.io/v1alpha1
kind: ManagementRedisCache
metadata:
  name: example
spec:
  forProvider:
    apiManagementId: ${azurerm_api_management.example.id}
    cacheLocation: East Us
    connectionStringSecretRef:
      key: attribute.primary_connection_string
      name: example-redis-cache
      namespace: crossplane-system
    description: Redis cache instances
    name: example-Redis-Cache
    redisCacheId: ${azurerm_redis_cache.example.id}
  providerConfigRef:
    name: example
