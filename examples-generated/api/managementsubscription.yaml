apiVersion: api.azure.jet.crossplane.io/v1alpha1
kind: ManagementSubscription
metadata:
  name: example
spec:
  forProvider:
    apiManagementName: ${data.azurerm_api_management.example.name}
    displayName: Parser API
    productId: ${data.azurerm_api_management_product.example.id}
    resourceGroupName: ${data.azurerm_api_management.example.resource_group_name}
    userId: ${data.azurerm_api_management_user.example.id}
  providerConfigRef:
    name: example
