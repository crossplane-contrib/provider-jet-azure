apiVersion: api.azure.jet.crossplane.io/v1alpha1
kind: ManagementTag
metadata:
  name: example
spec:
  forProvider:
    apiManagementId: ${azurerm_api_management.example.id}
    name: example-Tag
  providerConfigRef:
    name: example
