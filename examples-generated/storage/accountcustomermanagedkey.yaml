apiVersion: storage.azure.jet.crossplane.io/v1alpha1
kind: AccountCustomerManagedKey
metadata:
  name: example
spec:
  forProvider:
    keyName: generated-certificate
    keyVaultId: ${azurerm_key_vault.example.id}
    storageAccountId: ${azurerm_storage_account.example.id}
  providerConfigRef:
    name: example
