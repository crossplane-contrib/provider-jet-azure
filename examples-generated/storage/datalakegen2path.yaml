apiVersion: storage.azure.jet.crossplane.io/v1alpha1
kind: DataLakeGen2Path
metadata:
  name: example
spec:
  forProvider:
    filesystemName: example
    path: example
    resource: directory
    storageAccountId: ${azurerm_storage_account.example.id}
  providerConfigRef:
    name: example
