apiVersion: machine.azure.jet.crossplane.io/v1alpha1
kind: LearningSynapseSpark
metadata:
  name: example
spec:
  forProvider:
    identity:
    - type: SystemAssigned
    location: West Europe
    machineLearningWorkspaceId: ${azurerm_machine_learning_workspace.example.id}
    name: example
    synapseSparkPoolId: ${azurerm_synapse_spark_pool.example.id}
  providerConfigRef:
    name: example
