apiVersion: tenant.azure.jet.crossplane.io/v1alpha1
kind: TemplateDeployment
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: example
    templateSpecVersionId: ${data.azurerm_template_spec_version.example.id}
  providerConfigRef:
    name: example
