apiVersion: automation.azure.jet.crossplane.io/v1alpha1
kind: ConnectionClassicCertificate
metadata:
  name: example
spec:
  forProvider:
    automationAccountName: automationAccount1
    certificateAssetName: cert1
    name: connection-example
    resourceGroupName: ${azurerm_resource_group.example.name}
    subscriptionId: ${data.azurerm_client_config.example.subscription_id}
    subscriptionName: subs1
  providerConfigRef:
    name: example
