apiVersion: security.azure.jet.crossplane.io/v1alpha1
kind: CenterAssessment
metadata:
  name: example
spec:
  forProvider:
    assessmentPolicyId: ${azurerm_security_center_assessment_policy.example.id}
    status:
    - code: Healthy
    targetResourceId: ${azurerm_linux_virtual_machine_scale_set.example.id}
  providerConfigRef:
    name: example
