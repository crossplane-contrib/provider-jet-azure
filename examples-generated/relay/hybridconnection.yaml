apiVersion: relay.azure.jet.crossplane.io/v1alpha1
kind: HybridConnection
metadata:
  name: example
spec:
  forProvider:
    name: acctestrnhc-%d
    relayNamespaceName: example-relay
    requiresClientAuthorization: false
    resourceGroupName: ${azurerm_resource_group.example.name}
    userMetadata: testmetadata
  providerConfigRef:
    name: example
