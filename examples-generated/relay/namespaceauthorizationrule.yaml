apiVersion: relay.azure.jet.crossplane.io/v1alpha1
kind: NamespaceAuthorizationRule
metadata:
  name: example
spec:
  forProvider:
    listen: true
    manage: false
    name: example
    namespaceName: example-relay
    resourceGroupName: ${azurerm_resource_group.example.name}
    send: true
  providerConfigRef:
    name: example
