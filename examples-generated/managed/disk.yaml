apiVersion: managed.azure.jet.crossplane.io/v1alpha1
kind: Disk
metadata:
  name: example
spec:
  forProvider:
    createOption: Empty
    diskSizeGb: "1"
    location: West US 2
    name: acctestmd
    resourceGroupName: ${azurerm_resource_group.example.name}
    storageAccountType: Standard_LRS
    tags:
      environment: staging
  providerConfigRef:
    name: example
