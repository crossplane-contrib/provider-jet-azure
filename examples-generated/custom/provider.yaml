apiVersion: custom.azure.jet.crossplane.io/v1alpha1
kind: Provider
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: example_provider
    resourceGroupName: ${azurerm_resource_group.example.name}
    resourceType:
    - endpoint: https://testendpoint.com/
      name: dEf1
  providerConfigRef:
    name: example
