apiVersion: user.azure.jet.crossplane.io/v1alpha1
kind: AssignedIdentity
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: search-api
    resourceGroupName: ${azurerm_resource_group.example.name}
  providerConfigRef:
    name: example
