apiVersion: proximity.azure.jet.crossplane.io/v1alpha1
kind: PlacementGroup
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: exampleProximityPlacementGroup
    resourceGroupName: ${azurerm_resource_group.example.name}
    tags:
      environment: Production
  providerConfigRef:
    name: example
