apiVersion: eventgrid.azure.jet.crossplane.io/v1alpha1
kind: Topic
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: my-eventgrid-topic
    resourceGroupName: ${azurerm_resource_group.example.name}
    tags:
      environment: Production
  providerConfigRef:
    name: example
