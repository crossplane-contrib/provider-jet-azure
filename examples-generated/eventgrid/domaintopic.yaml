apiVersion: eventgrid.azure.jet.crossplane.io/v1alpha1
kind: DomainTopic
metadata:
  name: example
spec:
  forProvider:
    domainName: my-eventgrid-domain
    name: my-eventgrid-domain-topic
    resourceGroupName: ${azurerm_resource_group.example.name}
  providerConfigRef:
    name: example
