apiVersion: postgresql.azure.jet.crossplane.io/v1alpha1
kind: Configuration
metadata:
  name: example
spec:
  forProvider:
    name: backslash_quote
    resourceGroupNameRef:
      name: example
    serverNameRef:
      name: example
    value: "on"
  providerConfigRef:
    name: example
