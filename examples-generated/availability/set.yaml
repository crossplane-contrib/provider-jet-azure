apiVersion: availability.azure.jet.crossplane.io/v1alpha1
kind: Set
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: example-aset
    resourceGroupName: ${azurerm_resource_group.example.name}
    tags:
      environment: Production
  providerConfigRef:
    name: example
