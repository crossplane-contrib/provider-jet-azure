apiVersion: communication.azure.jet.crossplane.io/v1alpha1
kind: Service
metadata:
  name: example
spec:
  forProvider:
    dataLocation: United States
    name: example-communicationservice
    resourceGroupName: ${azurerm_resource_group.example.name}
  providerConfigRef:
    name: example
