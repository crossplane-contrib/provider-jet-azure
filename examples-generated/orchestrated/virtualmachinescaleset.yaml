apiVersion: orchestrated.azure.jet.crossplane.io/v1alpha1
kind: VirtualMachineScaleSet
metadata:
  name: example
spec:
  forProvider:
    location: West Europe
    name: example-VMSS
    platformFaultDomainCount: 1
    resourceGroupName: ${azurerm_resource_group.example.name}
    zones:
    - "1"
  providerConfigRef:
    name: example
