apiVersion: integration.azure.jet.crossplane.io/v1alpha1
kind: ServiceEnvironment
metadata:
  name: example
spec:
  forProvider:
    accessEndpointType: Internal
    location: West Europe
    name: example-ise
    resourceGroupName: ${azurerm_resource_group.example.name}
    skuName: Developer_0
    tags:
      environment: development
    virtualNetworkSubnetIds:
    - ${azurerm_subnet.isesubnet1.id}
    - ${azurerm_subnet.isesubnet2.id}
    - ${azurerm_subnet.isesubnet3.id}
    - ${azurerm_subnet.isesubnet4.id}
  providerConfigRef:
    name: example
