apiVersion: data.azure.jet.crossplane.io/v1alpha1
kind: FactoryLinkedServiceAzureSearch
metadata:
  name: example
spec:
  forProvider:
    dataFactoryId: ${azurerm_data_factory.example.id}
    name: example
    searchServiceKey: ${azurerm_search_service.example.primary_key}
    url: ${join("", ["https://", azurerm_search_service.example.name, ".search.windows.net"])}
  providerConfigRef:
    name: example
