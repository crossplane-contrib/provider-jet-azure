apiVersion: network.azure.tf.crossplane.io/v1alpha1
kind: LoadBalancer
metadata:
  name: example
spec:
  forProvider:
    location: East US
    name: example-lb
    resourceGroupNameRef:
      name: example
  providerConfigRef:
    name: example